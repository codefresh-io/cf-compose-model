const chai           = require('chai');
const sinonChai      = require('sinon-chai');
const CFComposeModel = require('./../CFComposeModel');
const policies       = require('./../policies');

const expect = chai.expect;
chai.use(sinonChai);



describe('', () => {
    it('', () => {
        const compose = new CFComposeModel();

    });
});

